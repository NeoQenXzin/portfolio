---
import type { CollectionEntry } from 'astro:content';

interface Props {
	project: CollectionEntry<'work'>;
}

const { data, slug } = Astro.props.project;
---

<a class="card" href={`/work/${slug}`}>
	<!-- <div class="container"> -->
		
		<span class="title">{data.title}</span>
		<!-- <div class="overlay"></div> -->
		<img src={data.img} alt={data.img_alt || ''} loading="lazy" decoding="async" />

	<!-- </div> -->
	</a>
		

<style>
	/* .container {
  position: relative;
	height: 100%;
	display: flex;
	justify-content: center;

} */

/* .overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0);
  transition: background 0.5s ease;
}

.container:hover .overlay {
  display: flex;
  background: rgba(0, 0, 0, .3);
} */


.title {
  position: absolute;
	margin-top: 0.5rem;
	display: flex;
	justify-content: center;
	align-items: center;
	/* height: 100%; */
	width: 90%;
  top: 0px;
  font-weight: 700;
  font-size: 30px;
  text-align: center;
	color: white;
  text-transform: uppercase;
  z-index: 1;
  transition: top .5s ease;
	background: top url('../../public/assets/backgrounds/background-nqx.jpg');
	border-radius: 12px;
	opacity: 0;

}

.container:hover .title {
	/* top: 90px; */
	/* height: 10%; */
	color: var(--gray-200);
	transition: all .5s ;

}



	.card {
		padding: 20px 10px;
		position: relative;
		display: grid;
		grid-template: auto 1fr / auto 1fr;
		height: 11rem;
		background: var(--gradient-subtle);
		border: 1px solid var(--gray-800);
		border-radius: 0.75rem;
		overflow: hidden;
		box-shadow: var(--shadow-sm);
		text-decoration: none;
		font-family: var(--font-brand);
		font-size: var(--text-lg);
		font-weight: 500;
		transition: box-shadow var(--theme-transition);
		display: flex;
		justify-content: center;
		transition: all .2s;

	}
	
	.card:hover {
		box-shadow: var(--shadow-md);
		padding: 0;
		transition: all .3s;
		/* filter: brightness(50%); */
	}
	.card:hover .title{
		/* color: var(--gray-200); */
		opacity: 1;
	/* height: 10%; */
	transition: all .5s ;
		/* filter: brightness(50%); */
	}


	/* .title {
		width: 100%;
		grid-area: 1 / 1 / 2 / 2;
		z-index: 1;
		margin: 0.5rem;
		padding: 0.5rem 1rem;
		background: url('../../public/assets/backgrounds/background-nqx.jpg');
		color: var(--gray-200);
		border-radius: 0.375rem;
		
	} */

	img {
		grid-area: 1 / 1 / 3 / 3;
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: 12px;
	}

	@media (min-width: 50em) {
		.card {
			height: 22rem;
			border-radius: 1.5rem;
		}

		.title {
			border-radius: 0.9375rem;
		}
	}
</style>
